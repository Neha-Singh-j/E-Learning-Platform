<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container">
    <!-- Brand / Logo -->
    <a class="navbar-brand d-flex align-items-center fw-bold" href="/">
      <i class="fas fa-graduation-cap me-2 text-warning fs-4"></i>
      <span class="brand-text">TechLearn</span>
    </a>

    <!-- Toggle button for mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Links -->
    <div class="collapse navbar-collapse" id="navbarMenu">
      <!-- Left nav -->
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link px-3" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3" href="/courses">Courses</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3" href="/instructors">Instructors</a>
        </li>
        <!-- <li class="nav-item">
          <a class="nav-link px-3" href="/pricing">Pricing</a>
        </li> -->
        <% if (currentUser && (currentUser.role === 'instructor' || currentUser.role === 'admin')) { %>
          <li class="nav-item">
            <a class="nav-link px-3" href="/courses/new">New Course</a>
          </li>
        <% } %>
      </ul>

      <!-- Right nav -->
      <div class="d-flex align-items-center gap-2">
        <!-- If not logged in -->
        <% if (!currentUser) { %>
          <a href="/login" class="btn btn-outline-light btn-sm px-3">Log In</a>
          <a href="/register" class="btn btn-primary btn-sm px-3">Sign Up</a>
        <% } else { %>
          <span class="text-white me-2">👋 Hello, <%= currentUser.username %></span>
          
          <!-- Cart -->
          <a href="/user/cart" class="btn btn-dark position-relative">
            <i class="fas fa-cart-shopping text-warning"></i>
            <% if (currentUser && currentUser.cart) { %>
              <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-warning text-dark">
                <%= currentUser.cart.length %>
              </span>
            <% } else { %>
              <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-secondary text-light">
                0
              </span>
            <% } %>
          </a>


          <!-- Logout -->
          <a href="/logout" class="btn btn-primary btn-sm px-3">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
